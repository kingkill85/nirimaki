# greeting.fish — silence the fish default welcome.
#
# Nirimaki's prompt and bar already identify the system; the default
# fish welcome (`Welcome to fish, the friendly interactive shell …`) is
# just noise.

set -U fish_greeting ''
